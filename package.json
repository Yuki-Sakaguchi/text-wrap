{
  "name": "dom",
  "version": "0.0.3",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -cw",
    "build:pro": "NODE_ENV=production rollup -c",
    "watch:pro": "NODE_ENV=production rollup -cw",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "jest": "^24.1.0",
    "rollup": "^1.1.2",
    "rollup-plugin-commonjs": "^9.2.0",
    "rollup-plugin-typescript2": "^0.19.2",
    "rollup-plugin-uglify": "^6.0.2",
    "tslib": "^1.9.3"
  },
  "dependencies": {
    "typescript": "^3.3.1"
  }
}
